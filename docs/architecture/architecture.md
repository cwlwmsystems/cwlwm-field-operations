# Architecture

Cwlwm Field Operations is tenant-first. Every business record is owned by an organization and access is granted through organization memberships and optional team memberships.

The application supports mock-data mode until Supabase is available. No employer-specific data is required to build or test the scaffold.
