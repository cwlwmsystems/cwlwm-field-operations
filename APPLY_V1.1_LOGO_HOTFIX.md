# Cwlwm Logo Hotfix

Replaces the temporary `C` brand mark in the top-left app shell with the supplied Cwlwm knot logo.

Files:
- `components/AppShell.tsx`
- `app/globals.css`
- `public/cwlwm-knot-logo.png`

After applying:

```powershell
npm run build
npm run dev
```
