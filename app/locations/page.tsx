"use client";
import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { usePlatformStore } from "@/lib/store/platformStore";
export default function Page(){const {data}=usePlatformStore();return <AppShell><div className="page-header"><div><div className="eyebrow">Territory Operations</div><h1>Locations</h1><p className="muted">Configured field locations for the current organization.</p></div><Link className="button" href="/admin/locations">Add / Import Locations</Link></div><div className="card table-card"><table><thead><tr><th>Address</th><th>Territory</th><th>Team</th><th>Disposition</th></tr></thead><tbody>{data.locations.map(l=><tr key={l.id}><td><Link className="table-link" href={`/locations/${l.id}`}>{l.address}</Link><div className="muted small">{l.city}, {l.state} {l.postalCode}</div></td><td>{data.territories.find(t=>t.id===l.territoryId)?.name||'Unassigned'}</td><td>{data.teams.find(t=>t.id===l.teamId)?.name||'Unassigned'}</td><td><span className="badge">{l.disposition}</span></td></tr>)}</tbody></table></div></AppShell>}
