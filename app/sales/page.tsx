"use client";
import Link from "next/link"; import {AppShell} from "@/components/AppShell"; import {useSupabaseConfig} from "@/lib/config/SupabaseConfigProvider"; import {useSupabaseSales} from "@/lib/sales/SupabaseSalesProvider";
export default function Page(){const {locations}=useSupabaseConfig();const {attempts,orders,loading,error}=useSupabaseSales();const active=attempts.filter(a=>a.status==="in_progress");const pending=orders.filter(o=>o.reviewStatus==="pending");return <AppShell><div className="page-header"><div><div className="eyebrow">Sales Operations · Supabase</div><h1>Sales</h1><p className="muted">Sales attempts and orders now persist in Supabase.</p></div><Link className="button" href="/locations">Start from a location</Link></div>{error&&<div className="error-banner"><strong>Sales load failed</strong><span>{error}</span></div>}<div className="grid metric-grid"><div className="card"><div className="eyebrow">In progress</div><div className="metric">{active.length}</div></div><div className="card"><div className="eyebrow">Orders submitted</div><div className="metric">{orders.length}</div></div><div className="card"><div className="eyebrow">Pending review</div><div className="metric">{pending.length}</div></div></div><section className="section-block"><div className="section-heading"><div><div className="eyebrow">Resume</div><h2>Open sales attempts</h2></div></div><div className="card table-card">{loading?<div className="empty-state">Loading sales…</div>:active.length===0?<div className="empty-state">No open attempts.</div>:<table><thead><tr><th>Customer</th><th>Location</th><th>Progress</th><th></th></tr></thead><tbody>{active.map(a=><tr key={a.id}><td>{[a.firstName,a.lastName].filter(Boolean).join(" ")||"Unnamed"}</td><td>{locations.find(l=>l.id===a.locationId)?.address??"Unknown"}</td><td>Step {a.progressStep}/4 · {a.progressStage}</td><td><Link className="text-link" href={`/sales/new/${a.locationId}?attempt=${a.id}`}>Resume</Link></td></tr>)}</tbody></table>}</div></section><section className="section-block"><div className="section-heading"><div><div className="eyebrow">Orders</div><h2>Recent submissions</h2></div><Link className="text-link" href="/admin/sales-review">Sales Review</Link></div><div className="card table-card">{orders.length===0?<div className="empty-state">No orders yet.</div>:<table><thead><tr><th>Customer</th><th>Product</th><th>Preferred install</th><th>Review</th><th></th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>{o.customerName}</td><td>{o.productNameSnapshot}<div className="muted small">{o.offerNameSnapshot}</div></td><td>{o.installDate||"—"} {o.installTime&&`· ${o.installTime}`}</td><td><span className="badge">{o.reviewStatus.replace("_"," ")}</span></td><td><Link className="text-link" href={`/sales/orders/${o.id}`}>View</Link></td></tr>)}</tbody></table>}</div></section></AppShell>}
