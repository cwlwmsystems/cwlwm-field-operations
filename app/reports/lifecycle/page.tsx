"use client";
import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useSupabaseLifecycle} from "@/lib/lifecycle/SupabaseLifecycleProvider";import {useSupabaseSales} from "@/lib/sales/SupabaseSalesProvider";
export default function Page(){const l=useSupabaseLifecycle(),s=useSupabaseSales();const rows=l.stages.map(stage=>({id:stage.id,name:stage.name,category:stage.category,terminal:stage.isTerminal,count:s.orders.filter(o=>l.getCurrentStage(o.id)?.id===stage.id).length}));return <AppShell><div className="breadcrumbs"><Link href="/reports">Reports</Link><span>/</span>Lifecycle</div><div className="page-header"><div><div className="eyebrow">Fulfillment · Supabase</div><h1>Lifecycle Report</h1></div></div><div className="card table-card"><table><thead><tr><th>Stage</th><th>Category</th><th>Orders</th><th>Terminal</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.category}</td><td>{r.count}</td><td>{r.terminal?"Yes":"No"}</td></tr>)}</tbody></table></div></AppShell>}
