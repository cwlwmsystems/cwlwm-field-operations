"use client";import Link from "next/link";import {AppShell} from "@/components/AppShell";import {usePlatformStore} from "@/lib/store/platformStore";import {financeSummary,money} from "@/lib/reporting/metrics";export default function Page(){const {data}=usePlatformStore();const s=financeSummary(data);return <AppShell><div className="breadcrumbs"><Link href="/reports">Reports</Link><span>/</span>Finance</div><div className="page-header"><div><div className="eyebrow">Finance</div><h1>Invoice & Adjustment Report</h1><p className="muted">Billing value, status, and adjustment impact.</p></div></div><div className="grid metric-grid"><div className="card"><div className="eyebrow">Batches</div><div className="metric">{s.batches}</div></div><div className="card"><div className="eyebrow">Subtotal</div><div className="metric">{money(s.subtotal)}</div></div><div className="card"><div className="eyebrow">Adjustments</div><div className="metric">{money(s.adjustments)}</div></div><div className="card"><div className="eyebrow">Total</div><div className="metric">{money(s.total)}</div></div></div><section className="card table-card section-block"><table><thead><tr><th>Invoice</th><th>Status</th><th>Orders</th><th>Subtotal</th><th>Adjustments</th><th>Total</th></tr></thead><tbody>{data.invoiceBatches.map(b=><tr key={b.id}><td><Link className="text-link" href={`/finance/invoices/${b.id}`}>{b.invoiceNumber}</Link></td><td>{b.status}</td><td>{b.orderIds.length}</td><td>{money(b.subtotal)}</td><td>{money(b.adjustmentsTotal)}</td><td>{money(b.total)}</td></tr>)}</tbody></table></section></AppShell>}