"use client";
import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useSupabaseFinance} from "@/lib/finance/SupabaseFinanceProvider";import {money} from "@/lib/reporting/liveMetrics";
export default function Page(){const f=useSupabaseFinance(),cur=f.settings?.currency??"USD";return <AppShell><div className="breadcrumbs"><Link href="/reports">Reports</Link><span>/</span>Finance</div><div className="page-header"><div><div className="eyebrow">Finance · Supabase</div><h1>Finance Report</h1></div></div><div className="grid metric-grid"><div className="card"><div className="eyebrow">Batches</div><div className="metric">{f.batches.length}</div></div><div className="card"><div className="eyebrow">Total value</div><div className="metric">{money(f.batches.filter(x=>x.status!=="void").reduce((s,x)=>s+f.getBatchTotal(x.id),0),cur)}</div></div><div className="card"><div className="eyebrow">PDF exports</div><div className="metric">{f.exports.filter(x=>x.format==="pdf").length}</div></div><div className="card"><div className="eyebrow">CSV exports</div><div className="metric">{f.exports.filter(x=>x.format==="csv").length}</div></div></div><div className="card table-card section-block"><table><thead><tr><th>Invoice</th><th>Status</th><th>Subtotal</th><th>Adjustments</th><th>Total</th><th>Exports</th><th></th></tr></thead><tbody>{f.batches.map(b=><tr key={b.id}><td><strong>{b.invoiceNumber}</strong></td><td>{b.status}</td><td>{money(b.subtotal,cur)}</td><td>{money(b.adjustmentsTotal,cur)}</td><td>{money(f.getBatchTotal(b.id),cur)}</td><td>{f.exports.filter(x=>x.batchId===b.id).length}</td><td><Link className="text-link" href={`/finance/invoices/${b.id}`}>Open</Link></td></tr>)}</tbody></table></div></AppShell>}
