"use client";import Link from "next/link";import {AppShell} from "@/components/AppShell";import {usePlatformStore} from "@/lib/store/platformStore";export default function Page(){const {data}=usePlatformStore();return <AppShell><div className="breadcrumbs"><Link href="/reports">Reports</Link><span>/</span>Scheduling</div><div className="page-header"><div><div className="eyebrow">Scheduling</div><h1>Appointment Outcomes</h1><p className="muted">Appointment status by territory.</p></div></div><div className="card table-card"><table><thead><tr><th>Territory</th><th>Booked</th><th>Completed</th><th>Cancelled</th><th>No show</th></tr></thead><tbody>{data.territories.map(t=>{const a=data.appointments.filter(x=>x.territoryId===t.id);return <tr key={t.id}><td>{t.name}</td><td>{a.filter(x=>["booked","confirmed","rescheduled"].includes(x.status)).length}</td><td>{a.filter(x=>x.status==="completed").length}</td><td>{a.filter(x=>x.status==="cancelled").length}</td><td>{a.filter(x=>x.status==="no_show").length}</td></tr>})}</tbody></table></div></AppShell>}