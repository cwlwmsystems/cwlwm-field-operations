"use client";
import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useSupabaseConfig} from "@/lib/config/SupabaseConfigProvider";
export default function Page(){const {teams,reps,territories,loading}=useSupabaseConfig();return <AppShell><div className="page-header"><div><div className="eyebrow">Operations · Supabase</div><h1>Teams</h1><p className="muted">Internal, vendor, and partner field groups.</p></div><Link className="button" href="/admin/teams">Manage Teams</Link></div><div className="card table-card">{loading?<div className="empty-state">Loading teams…</div>:<table><thead><tr><th>Team</th><th>Type</th><th>Active Reps</th><th>Territories</th></tr></thead><tbody>{teams.map(t=><tr key={t.id}><td><strong>{t.name}</strong></td><td><span className="badge">{t.type}</span></td><td>{reps.filter(r=>r.teamId===t.id&&r.status==="active").length}</td><td>{territories.filter(x=>x.teamId===t.id).length}</td></tr>)}</tbody></table>}</div></AppShell>}
