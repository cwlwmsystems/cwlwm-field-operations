"use client";
import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useSupabaseConfig} from "@/lib/config/SupabaseConfigProvider";
export default function Page(){const {organization}=useSupabaseConfig();return <AppShell><div className="page-header"><div><div className="eyebrow">Organization · Supabase</div><h1>{organization?.name??"Organization"}</h1><p className="muted">Active tenant configuration loaded through RLS.</p></div><Link className="button" href="/admin/organization">Edit Settings</Link></div><div className="grid location-summary-grid"><div className="card"><div className="eyebrow">Slug</div><div className="metric-small">{organization?.slug??"—"}</div></div><div className="card"><div className="eyebrow">Timezone</div><div className="metric-small">{organization?.timezone??"—"}</div></div><div className="card"><div className="eyebrow">Status</div><div className="metric-small">{organization?.status??"—"}</div></div></div></AppShell>}
