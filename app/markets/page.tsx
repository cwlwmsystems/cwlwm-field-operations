"use client";
import Link from "next/link";import {AppShell} from "@/components/AppShell";import {useSupabaseConfig} from "@/lib/config/SupabaseConfigProvider";
export default function Page(){const {markets,territories,loading}=useSupabaseConfig();return <AppShell><div className="page-header"><div><div className="eyebrow">Operations · Supabase</div><h1>Markets</h1><p className="muted">Geographic groupings used to organize territories.</p></div><Link className="button" href="/admin/markets">Manage Markets</Link></div><div className="card table-card">{loading?<div className="empty-state">Loading markets…</div>:<table><thead><tr><th>Market</th><th>Region</th><th>Territories</th><th>Status</th></tr></thead><tbody>{markets.map(m=><tr key={m.id}><td><strong>{m.name}</strong><div className="muted small">{m.slug}</div></td><td>{m.stateRegion||"—"}</td><td>{territories.filter(t=>t.marketId===m.id).length}</td><td><span className="badge">{m.status}</span></td></tr>)}</tbody></table>}</div></AppShell>}
