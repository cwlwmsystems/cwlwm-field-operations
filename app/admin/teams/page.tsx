"use client";
import { FormEvent, useState } from "react";
import { AppShell } from "@/components/AppShell"; import { AdminNav } from "@/components/admin/AdminNav";
import { makeId, usePlatformStore } from "@/lib/store/platformStore"; import type { DemoTeam } from "@/lib/mock/data";
const blank: DemoTeam = { id:"", name:"", type:"internal", reps:0, status:"active" };
export default function Page(){ const {data,saveTeam,deleteTeam}=usePlatformStore(); const [editing,setEditing]=useState<DemoTeam>(blank);
 function submit(e:FormEvent){e.preventDefault(); saveTeam({...editing,id:editing.id||makeId("team"),name:editing.name.trim(),reps:data.reps.filter(r=>r.teamId===editing.id).length});setEditing(blank)}
 return <AppShell><div className="eyebrow">Admin</div><h1>Teams</h1><AdminNav/><div className="admin-split"><form className="card admin-form single" onSubmit={submit}><h2>{editing.id?"Edit Team":"Create Team"}</h2><label>Name<input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})} required/></label><label>Type<select value={editing.type} onChange={e=>setEditing({...editing,type:e.target.value as DemoTeam["type"]})}><option value="internal">Internal</option><option value="vendor">Vendor / Partner</option></select></label><label>Status<select value={editing.status} onChange={e=>setEditing({...editing,status:e.target.value as DemoTeam["status"]})}><option value="active">Active</option><option value="inactive">Inactive</option></select></label><div className="form-actions"><button className="button">{editing.id?"Save Changes":"Create Team"}</button>{editing.id&&<button type="button" className="button secondary" onClick={()=>setEditing(blank)}>Cancel</button>}</div></form><div className="card table-card"><table><thead><tr><th>Team</th><th>Type</th><th>Reps</th><th>Status</th><th></th></tr></thead><tbody>{data.teams.map(t=><tr key={t.id}><td><strong>{t.name}</strong></td><td><span className="badge">{t.type}</span></td><td>{data.reps.filter(r=>r.teamId===t.id).length}</td><td><span className="badge">{t.status}</span></td><td className="row-actions"><button onClick={()=>setEditing(t)}>Edit</button><button className="danger-link" onClick={()=>confirm(`Delete ${t.name}?`)&&deleteTeam(t.id)}>Delete</button></td></tr>)}</tbody></table></div></div></AppShell> }
