"use client";
import { FormEvent,useState } from "react"; import { AppShell } from "@/components/AppShell"; import { AdminNav } from "@/components/admin/AdminNav"; import { makeId,usePlatformStore } from "@/lib/store/platformStore"; import type { DemoTerritory } from "@/lib/mock/data";
const blank:DemoTerritory={id:"",name:"",market:"",team:"",teamId:"",locations:0,status:"active",description:""};
export default function Page(){const {data,saveTerritory,deleteTerritory}=usePlatformStore();const [editing,setEditing]=useState<DemoTerritory>(blank);function submit(e:FormEvent){e.preventDefault();const team=data.teams.find(t=>t.id===editing.teamId);saveTerritory({...editing,id:editing.id||makeId("terr"),team:team?.name||"Unassigned",locations:data.locations.filter(l=>l.territoryId===editing.id).length});setEditing(blank)}return <AppShell><div className="eyebrow">Admin</div><h1>Territories</h1><AdminNav/><div className="admin-split"><form className="card admin-form single" onSubmit={submit}><h2>{editing.id?"Edit Territory":"Create Territory"}</h2><label>Name<input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})} required/></label><label>Market<select value={editing.market} onChange={e=>setEditing({...editing,market:e.target.value})} required><option value="">Select market…</option>{data.markets.map(m=><option key={m.id} value={m.name}>{m.name}</option>)}</select></label><label>Primary Team<select value={editing.teamId} onChange={e=>setEditing({...editing,teamId:e.target.value})}><option value="">Unassigned</option>{data.teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label><label>Status<select value={editing.status} onChange={e=>setEditing({...editing,status:e.target.value as DemoTerritory["status"]})}><option value="active">Active</option><option value="inactive">Inactive</option></select></label><label>Description<textarea rows={4} value={editing.description} onChange={e=>setEditing({...editing,description:e.target.value})}/></label><div className="form-actions"><button className="button">Save Territory</button>{editing.id&&<button type="button" className="button secondary" onClick={()=>setEditing(blank)}>Cancel</button>}</div></form><div className="card table-card"><table><thead><tr><th>Territory</th><th>Market</th><th>Team</th><th></th></tr></thead><tbody>{data.territories.map(t=><tr key={t.id}><td><strong>{t.name}</strong><div className="muted small">{t.status}</div></td><td>{t.market}</td><td>{data.teams.find(x=>x.id===t.teamId)?.name||"Unassigned"}</td><td className="row-actions"><button onClick={()=>setEditing(t)}>Edit</button><button className="danger-link" onClick={()=>confirm(`Delete ${t.name}?`)&&deleteTerritory(t.id)}>Delete</button></td></tr>)}</tbody></table></div></div></AppShell>}
