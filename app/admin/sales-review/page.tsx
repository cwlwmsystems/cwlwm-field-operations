"use client";
import Link from "next/link";import {useState} from "react";import {AppShell} from "@/components/AppShell";import {AdminNav} from "@/components/admin/AdminNav";import {useSupabaseConfig} from "@/lib/config/SupabaseConfigProvider";import {useSupabaseSales} from "@/lib/sales/SupabaseSalesProvider";
export default function Page(){const {locations,reps}=useSupabaseConfig();const {orders,reviewOrder}=useSupabaseSales();const [filter,setFilter]=useState("all");const rows=orders.filter(o=>filter==="all"||o.reviewStatus===filter);return <AppShell><div className="page-header"><div><div className="eyebrow">Admin · Supabase</div><h1>Sales Review</h1><p className="muted">Review status writes directly to Supabase.</p></div></div><AdminNav/><div className="review-toolbar"><label>Status<select value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">All</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="needs_attention">Needs attention</option></select></label></div><div className="card table-card">{rows.length===0?<div className="empty-state">No matching orders.</div>:<table><thead><tr><th>Customer</th><th>Location</th><th>Product</th><th>Rep</th><th>Review</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td><strong>{o.customerName}</strong></td><td>{locations.find(l=>l.id===o.locationId)?.address??"Unknown"}</td><td>{o.productNameSnapshot}</td><td>{reps.find(r=>r.id===o.representativeId)?.name??"Unknown"}</td><td><span className="badge">{o.reviewStatus.replace("_"," ")}</span></td><td className="row-actions"><Link className="text-link" href={`/sales/orders/${o.id}`}>View</Link><button onClick={()=>reviewOrder(o.id,"approved","Approved in Sales Review.")}>Approve</button><button onClick={()=>reviewOrder(o.id,"flagged","Flagged for follow-up.")}>Flag</button></td></tr>)}</tbody></table>}</div></AppShell>}
