"use client";import {AppShell} from "@/components/AppShell";import {AdminNav} from "@/components/admin/AdminNav";export default function Page(){return <AppShell><div className="page-header"><div><div className="eyebrow">Administration</div><h1>Reporting Settings</h1><p className="muted">Reporting currently recalculates directly from operational records.</p></div></div><AdminNav/><div className="grid two-column section-block"><section className="card"><div className="eyebrow">Prototype mode</div><h2>Live local metrics</h2><p className="muted">Reports use the same browser-local store as Sales, Scheduling, Lifecycle, and Finance, so operational changes appear immediately.</p></section><section className="card"><div className="eyebrow">Production direction</div><h2>Backend analytics</h2><p className="muted">When Supabase is connected, larger tenants should use organization-scoped SQL views or aggregate tables instead of loading every operational row into the browser.</p></section></div></AppShell>}