"use client";
import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { usePlatformStore } from "@/lib/store/platformStore";
export default function Page(){const {data}=usePlatformStore();return <AppShell><div className="page-header"><div><div className="eyebrow">Territory Operations</div><h1>Territories</h1><p className="muted">Open a territory to view assigned representatives and field locations.</p></div><Link className="button" href="/admin/territories">Manage Territories</Link></div><div className="card table-card"><table><thead><tr><th>Territory</th><th>Market</th><th>Team</th><th>Locations</th><th>Status</th></tr></thead><tbody>{data.territories.map(t=><tr key={t.id}><td><Link className="table-link" href={`/territories/${t.id}`}>{t.name}</Link></td><td>{t.market}</td><td>{data.teams.find(x=>x.id===t.teamId)?.name||'Unassigned'}</td><td>{data.locations.filter(l=>l.territoryId===t.id).length}</td><td><span className={`badge ${t.status==='active'?'success':'neutral'}`}>{t.status}</span></td></tr>)}</tbody></table></div></AppShell>}
